# multi-model-agentflow 协作约定

## 项目目标

本项目设计一套平台无关、可复用的风险感知型多模型协作系统，用于任务路由、执行、验证、审核、升级和恢复。Codex Skill 是第一个入口，但通用核心不得依赖某个模型、供应商、Agent Engine 或业务领域。

## 当前阶段

用户已于 2026-09-04 明确开启 MVP 实现阶段。当前约束为：

- 按 `task_plan.md` 的里程碑推进，每完成一个里程碑就向用户反馈验证结果；
- 只实现 `docs/mvp.md` 明确列入范围的能力，不扩展到远程真实适配器、网页控制台或复杂并行调度；
- 使用无费用测试替身和本地模型路径，未经新的明确授权不得调用收费模型或产生 API 费用；
- 当前任务不启动子 Agent；
- 未经用户要求不创建 Git 提交。

## 文档职责与优先级

- `docs/requirements.md` 是产品需求的规范来源，使用稳定编号描述必须满足的行为。
- `docs/mvp.md` 定义第一版实现边界和可执行验收场景；它可以收缩需求，但不得改写需求。
- `docs/architecture-decisions.md` 记录已接受、暂定和待决定的架构选择，也是待确认事项的唯一清单。
- 本文件只规定项目内协作方式，不复制详细产品需求。

发生冲突时，优先级为：用户最新明确决定 > `requirements.md` > 已接受的架构决策 > `mvp.md` 中的实现建议。发现冲突时先记录，不以“合理推断”替用户定案。

## 不可破坏的边界

- 明确区分通用核心与项目级策略；股票投研仅是首个试验项目，金融规则不得写入通用核心。
- 模型名称、版本、供应商和能力是注册表数据，不是 Skill 或核心中的永久默认值。
- Skill 可以被自动推荐，但任何模型执行都必须建立在明确激活和有效授权之上。
- 授权绑定计划版本与内容哈希；超出模型、预算、文件、数据或副作用范围必须重新授权。
- 操作安全边界不能因用户降低业务重要度而取消。
- D3 数据和密钥不得发送给远程模型；密钥不得写入仓库、日志或模型上下文。
- 同一模型不得包办实施、自测、审核和宣布完成；审核默认只读。
- 控制平面是确定性程序和可停用边车，不是 AI 模型，也不是所有普通任务的强制依赖。
- 等待优先使用事件和非 LLM 状态检查，不以高频模型调用轮询。
- 中断恢复必须避免重复已完成任务和已完成的收费调用。

## Codex Skill 设计约定

Skill 实现遵循 `skill-creator` 的渐进披露原则：

- `SKILL.md` 只保留识别适用场景所需的描述、关键授权边界和入口工作流；
- 大段模式说明、任务合同或策略说明只有在确有需要时才拆入 `references/`，并由 `SKILL.md` 指明何时读取；
- 只有可重复且需要确定性的操作才进入 `scripts/`；不得复制控制平面逻辑；
- `agents/openai.yaml` 只承载必要的界面元数据与调用策略；默认允许自动发现，但自动发现只能建议启用，不能等同于执行授权；
- 不创建 README、安装指南、示例或资源目录，除非出现明确用途；
- Skill 调用与 Terminal 必须使用同一套计划、状态和授权记录，不能形成第二套状态源。

## 修改与验证约定

- 先阅读全部相关文档，再修改共享术语、状态、字段或范围。
- 新需求应有唯一编号、可观察行为和验收证据；避免把愿景、建议或实现细节写成强制需求。
- 暂定选择必须标注“暂定”或“待确认”，不能悄然升级为已接受决策。
- 文档改动后检查：需求编号引用有效、MVP 范围与非目标一致、待确认事项无重复、四份文档无矛盾。
- 实现变更必须保留测试、审核、费用、授权和状态证据；测试替身记录必须明确标记，不得伪装成真实模型运行。
